# Core Layer

This directory contains the FFmpeg integration and video processing logic.

## Responsibilities

- Video/audio file import and validation
- FFmpeg operations (trim, merge, export)
- File format conversion
- Video metadata extraction

## Dependencies

- FFmpeg (system dependency)
- Node.js FFmpeg bindings
